# `graphed` is pinned to the same release as the CI docs job (env.GRAPHED) so the docs build against
# the version these pages were written against.
# The [awkward,numpy] extras supply the backends that graphed-histogram's autosummary imports at
# doc-build time.
graphed[awkward,numpy]==0.0.5
